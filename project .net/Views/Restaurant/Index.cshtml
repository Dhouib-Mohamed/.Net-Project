@model List<Restaurant>

@{
    ViewData["Title"] = "Home Page";
}



<div id="carouselExampleControlsNoTouching" class="carousel slide align-content-center" data-bs-ride="carousel">
    <div class="carousel-inner">
        @for (int i = 0 ; i<Model.Count ; i++)
        {
            if (i==0) {
            <div class="carousel-item active ">
                    <img src=@Model[i].image alt=@Model[i].Name>
            </div>}
            else
            {
                <div class="carousel-item">
                    <img src=@Model[i].image alt=@Model[i].Name>
                </div>
            }
        }
    </div>
   
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
    </div>
 
    
    
@* ou bien 
<div class="row">
    @foreach (var r in Model){
    <div class="col-sm-6">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@r.Name</h5>
                    <img  src=@r.image alt=@r.Name>
                <a href="#" class="btn btn-primary">Go somewhere</a>
            </div>
        </div>
        </div>
    }

</div>
*@ 
<div class="row">
    @foreach (var r in Model){
        <div class="col-sm-3 p-3" >
            <div class="card align-content-around" style="height: 380px;">
                <img class="card-img-top" src=@r.image alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@r.Name</h5>
                    <a href="/Restaurant/restaurant_detail/@r.Id"><button class="btn btn-primary"> Detail</button> </a>
                </div>
            </div>
        </div>
    }

</div>
